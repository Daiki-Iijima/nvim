-- =============================================================================
-- nvim-ts-autotag: HTML/JSX/TSX タグの自動閉じ・自動リネーム
-- =============================================================================
-- Treesitter を利用して、HTML タグを入力したときに閉じタグを自動生成し、
-- 開始タグの名前を変更した際にも閉じタグの名前を自動で同期して変更します。
-- =============================================================================

return {
  "windwp/nvim-ts-autotag",
  event = { "BufReadPre", "BufNewFile" },
  opts = {},
}
